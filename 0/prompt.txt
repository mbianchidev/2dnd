In /Users/mbianchidev/.copilot/copilot-worktrees/2dnd/cp-desktop-laughing-system, run the remaining full release validation without modifying files: `npm test`, then `npm run test:browser`, then `npm run build`, then `git diff --check`, then `git status --short --branch`. Stop on the first failure and return the full relevant output. On success, return concise counts/results for Vitest and Playwright plus build/diff/status. Do not commit, push, or edit anything.